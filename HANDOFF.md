# 인수인계 — Quest on Naju

작성일 2026-08-07 · 이 저장소는 `jihoonlove` 모노레포의 `apps/naju-heritage-quest`를
분리해 온 독립 저장소입니다 (이력 보존, 커밋 해시는 분리 과정에서 바뀌었습니다)

> **한 줄 요약:** P1 수직 슬라이스가 완주 가능합니다. 다시초등학교 → 영산강
> 뱃길 → 복암리 고분군 → 퀘스트 3개 + 층위 퍼즐 → 엔딩까지 한 줄로 이어집니다.
> 다음 할 일은 **복암리 학습 콘텐츠 전문가 검수**와 **발굴 미니게임**입니다.

---

## 1. 로컬에서 돌리기

### 준비물
- **Node 20 이상** (개발·검증은 22.22.2에서 했습니다)
- npm 10 이상 (Node에 딸려옵니다)

### 실행

```bash
git clone https://github.com/reinhardt7177-lab/naju-heritage-quest.git
cd naju-heritage-quest

npm install
npm run dev   # → http://localhost:5173/
```

### 그 밖의 명령

| 명령 | 하는 일 |
| --- | --- |
| `npm run dev` | 개발 서버 (HMR) · http://localhost:5173 |
| `npm run build` | 타입 체크 + 프로덕션 빌드 → `dist/` |
| `npm run typecheck` | 타입만 검사 |
| `npm run build:artifact` | 자기완결적 단일 HTML → `artifact/dasi-school.html` |

### 조작

| | |
| --- | --- |
| 이동 | 조이스틱 드래그 · `W A S D` · 방향키 |
| 카메라 회전 | 화면 빈 곳 드래그 · `↺ ↻` 버튼 · `Q` `E` |
| 탑뷰 | `탑뷰` 버튼 · `T` |
| 카메라 리셋 | `⟲` 버튼 · `R` |
| 상호작용 | `Space` · `F` · `말 걸기·살펴보기` 버튼 |
| 나주 지도 | `M` · 오른쪽 위 「나주 지도」 버튼 |
| 저사양 모드 | 좌하단 `⚡ 저사양 모드` |

### 해볼 것 (5분 완주 코스)

1. 앞의 금색 `!` 담임 선생님에게 다가가 이야기를 끝까지 듣기 → 퀘스트 수락
2. 다시 말 걸기 (`?` 표시) → **XP 80**, 오른쪽 위에 「나주 지도」가 생김
3. `M` → 복암리 선택 → **영산강 뱃길** 연출
4. 마을 어르신에게 퀘스트를 받고, 안내판을 읽고, 3호분 정상까지 올라가기
5. 발굴단장에게 보고 → **흙손** 획득 → 트렌치에서 층위 표지 3개 확인
6. 흙손으로 트렌치를 살펴 **층위 퍼즐** 풀기 → 발굴단장에게 보고
7. 연구원에게 옹관 퀘스트를 받고 항아리 둘을 살펴본 뒤 보고 → **엔딩**

## 2. 지금까지 한 일

### 2.1 저장소 구조 — 독립 저장소

원래 `jihoonlove` 모노레포의 `apps/naju-heritage-quest`였는데, 두 앱이
코드를 전혀 공유하지 않아 **앱별 독립 저장소로 분리**했습니다.
「나주를 빛낸 인물들」은 `naju-heroes` 저장소에 있습니다.

### 2.2 기획 문서 (`docs/`)

| 문서 | 내용 |
| --- | --- |
| `00-PLAN.md` | 전체 요약 — **여기서 시작** |
| `01-RESEARCH.md` | 레퍼런스 리서치. **국가유산청 무료 3D 에셋 545건**(2026-06 개방)이 핵심 발견 |
| `02-GAME-DESIGN.md` | 퀘스트 6종 · 퍼즐 아키타입 6종 · 성장/장비 · 온보딩 5분 |
| `03-TECH-ARCHITECTURE.md` | 아키텍처 + 성능 예산 + **로우폴리 3D RPG 구현 가능성 판정** |
| `04-CONTENT-bokamri.md` | 복암리 고분군 콘텐츠 설계 (퀘스트 체인 초안) |
| `05-ROADMAP.md` | P0~P4 단계와 판단 게이트 |
| `06-NAJU-WORLD-MAP.md` | **나주 지도 분석 + 지역별 맵 분리 설계** |

### 2.3 다시초등학교 맵 — 동작합니다

오너가 준 **항공 영상(2024-10-30)** 을 근거로 만들었습니다.
처음엔 "농촌 초등학교의 전형"을 가정해 흙 운동장 하나짜리로 만들었다가
실제와 크게 달라 전면 수정했습니다.

**구현된 것**
- 본관: 분홍 벽돌 3층 + 흰 수평 띠, 서쪽 날개가 꺾이고 접점에 **은색 돔 탑**
- 강당: 빨간 반원통(아치) 지붕 별동
- 운동장 둘: 잔디 운동장 + **빨간 우레탄 트랙**(파란 안쪽 띠 · 흙 인필드)
- 주차장, 파란 지붕 창고, 교정 활엽수 군락, 향나무 줄, 화단
- 국기게양대 — **태극기**(국기법 시행령 규격, 런타임 캔버스로 그림)
- 학교 밖: 다시면 소재지 마을 + 논 + 원경 산줄기
- 플레이어: 로우폴리 학생(낡은 체육복 + 책가방), 절차적 걷기 애니메이션
- 담임 선생님 NPC: 금색 `!`, 플레이어를 바라봄, 5단 대화 → 퀘스트 수락 + XP
- 카메라: 3인칭 쿼터뷰, 좌우 회전, 탑뷰 전환, 리셋
- 입력: 가상 조이스틱 + WASD (카메라 기준으로 방향 변환)
- HUD: 지역명 · 레벨/칭호 · 경험치 바 · 학습내용 배지 · 퀘스트 로그 · 대화창
- 저사양 모드 (그림자 off · DPR 1 · 안티앨리어싱 off)

**빌드 크기:** gzip 약 306KB (초기 다운로드 8MB 예산 대비 여유)

### 2.4 내린 기술 결정

| 결정 | 이유 |
| --- | --- |
| **Rapier(물리엔진) 안 씀** | 평지 + 점프 없음이라 대부분이 낭비. WASM 1MB를 아끼는 쪽이 "모바일에서 몇 초에 뜨는가"라는 P0 질문에 더 정직. 대신 AABB 충돌 직접 구현(`src/lib/collision.ts`). 복암리 구릉 지형에서 교체하려고 의존성은 남겨둠 |
| **한글은 DOM 오버레이로** | 3D SDF 텍스트를 쓰면 한글 폰트를 받아야 함. drei `Html`이면 폰트 에셋 0 |
| **태극기는 런타임 캔버스** | 이미지 파일이 늘면 로딩 경로도 늘고 아티팩트 배포에선 인라인 처리까지 따라옴 |
| **매 프레임 값은 스토어 밖에** | 캐릭터 위치·카메라를 zustand에 넣으면 리렌더로 프레임이 깎임 |
| **콘텐츠와 코드 분리** | `src/content/`는 순수 데이터. 새 유적 추가 시 `src/game/`을 안 건드려야 함 |

### 2.5 커밋 이력

```
README에 배포 방법 정리
카메라 회전·탑뷰 추가, 국기를 태극기로 교체
단일 HTML 아티팩트 빌드 추가 (플레이 테스트용)
다시초등학교 필드 구현 + 나주 월드맵 지역 분리 설계
모노레포 구조 분리 + 2번째 앱 「나주 문화유산 퀘스트」 기획
```

(모노레포에서 분리하면서 커밋 해시가 바뀌어 메시지만 적습니다. `git log`로 보세요.)

---

## 3. 앞으로 할 일

### 3.1 끝난 것 — 씬 레지스트리 + 월드맵 (완료)

두 번째 맵을 만들기 전에 넣어야 했던 뼈대가 들어갔습니다.

- `src/game/world/registry.ts` — 맵을 데이터로 등록. 씬은 **동적 import** 라
  복암리에 안 간 사람은 복암리 청크(약 5KB)를 받지 않습니다
- `src/store/mapStore.ts` — 현재 맵 · 방문 기록 · `travelTo()` · 뱃길 연출 시간
- `src/game/world/terrain.ts` — 맵이 지켜야 할 지형 계약(경계·충돌·spawn·높이).
  `Player.tsx` 가 특정 맵의 layout 을 직접 import 하지 않습니다
- `src/ui/WorldMap.tsx` — 기울인 나주 지도판 + 영산강 + 지역 아이콘,
  그리고 **나룻배 이동 연출**(3.6초). 그 사이에 다음 씬 청크가 내려옵니다
- 씬 파일은 `src/game/world/maps/` 아래로 옮겼습니다

### 3.2 끝난 것 — 복암리 고분군 맵 (완료)

- [x] 고분군 야외 씬. 구릉은 **높이 함수 하나**로 처리했습니다
      (`maps/bokamri/layout.ts` 의 `heightAt`). 보이는 지면과 걷는 높이가
      같은 식에서 나오므로 발이 뜨거나 잠기지 않습니다. Rapier 는 아직 불필요
- [x] 분구 4기 · 3호 트렌치(층 단면) · 옹관 전시 · 발굴 캠프 · 선착장 · 안내판 5
- [x] NPC 3인 — 마을 어르신 / 발굴단장 / 연구원
- [x] 퀘스트 3개가 **JSON 에서** 돌아갑니다. 목표 추적 · 보상 · 도감 · 흙손
- [x] 층위 퍼즐 (`src/ui/StratigraphyPuzzle.tsx`) — 정답 근거는 안내판 2번과
      트렌치 벽에 실제로 있습니다
- [x] 엔딩 (`src/ui/Ending.tsx`) → 학교로 돌아가기
- [x] localStorage 저장/불러오기 (진행 · 현재 맵)

### 3.3 다음 — 남은 것

- [ ] **복암리 학습 콘텐츠 전문가 검수** ← 공개 전 필수. 아래 4절 참조
- [ ] **국가유산청 3D 에셋 545건에서 나주 관련 항목 실제 조사**
      https://digital.khs.go.kr/heritage/heriTage.do
- [ ] 발굴 미니게임 (곡괭이/흙손/솔 3종, 손상 게이지)
- [ ] 토기 복원 퍼즐 · 3호분 챔버 방탈출 (`bokamri-05-layers`)
- [ ] 도감 UI (지금은 엔딩 카드에 이름만 나열)
- [ ] 실기기 성능 측정

### 3.4 P1 완료 기준 (진짜 판단 지점)

> 처음 열어서 복암리를 완주하고 Lv.10에 도달할 수 있고,
> **학생 5명 플레이테스트에서 "옹관묘가 뭐야?"에 답할 수 있을 것.**

여기서 재미없으면 유적을 더 늘려도 재미없습니다. 멈추고 판단하세요.

### 3.5 이후

`docs/05-ROADMAP.md` 참조. P2 월드 확장(유적 4곳) → P3 다듬기·배포 → P4 선택
(교사용 대시보드 > 계정 > 멀티플레이 순).

---

## 4. 미해결 · 확인 필요

| 항목 | 상태 |
| --- | --- |
| **다시초 배치 치수** | 항공 영상 **눈대중**입니다. 실측 아님. 비율은 맞췄고, 실측치를 얻으면 `src/game/world/layout.ts` 상수만 고치면 됩니다 — 다른 코드가 전부 거기를 참조하므로 모델링을 다시 할 필요 없음 |
| **복암리 학습 콘텐츠** | `docs/04-CONTENT-bokamri.md`와 `bokamri.quests.json`은 **제 기획 초안**입니다. 옹관묘·마한 전환기 서술은 검증 전이라 **그대로 게임에 넣으면 안 됩니다.** 국립나주박물관 발굴조사 보고서로 전수 검증 필요 |
| **실기기 성능** | 아직 못 쟀습니다. 헤드리스 측정치는 GPU 없는 소프트웨어 렌더러 기준이라 기기 지표가 아닙니다. **중급 안드로이드 태블릿에서 30fps 이상**이 P0 통과 기준 |
| **월드맵 좌표** | `worldMapPos` 값은 미정. 각 유산의 실제 좌표 확인 필요 |
| **다도면·봉황면 접근 경로** | 영산강 뱃길로 닿는 곳이 맞는지 미확인. 아닐 가능성이 높고, 그러면 육로 이동 연출을 따로 둬야 합니다 |
| **에셋 라이선스** | 국가유산청 데이터는 **공공누리 유형이 항목마다 다릅니다.** 받을 때마다 기록해야 하며, 스키마의 `license` 필드로 강제하고 있습니다 |

---

## 5. 배포

### Vercel (GitHub 연동)

`vercel.json`이 저장소 루트에 있어 대시보드에서 빌드 설정을 만질 필요가 없습니다.

1. https://vercel.com/new → `reinhardt7177-lab/naju-heritage-quest` Import
2. Project Name: `travelnaju` (원하는 이름으로)
3. Root Directory는 기본값 그대로. 나머지 설정은 `vercel.json`이 다 합니다

### 플레이 테스트용 단일 HTML

```bash
npm run build:artifact
```

외부 요청이 차단된 환경에 올릴 때 씁니다. 코드 스플리팅을 끄고 JS를 HTML에
인라인해 약 1MB짜리 자기완결 파일을 만듭니다. 생성물은 커밋하지 않습니다.

---

## 6. 코드 지도

```
src/
├── App.tsx                  Canvas + UI 오버레이 + 저사양 모드
├── main.tsx
├── game/
│   ├── player/
│   │   ├── Player.tsx       캐릭터 · 이동 · 걷기 애니메이션 · 카메라 추적
│   │   └── camera.ts        카메라 상태(yaw/탑뷰) · 시점 보간
│   ├── quest/QuestMarker.tsx  느낌표 마커 (폰트 없이 박스 2개)
│   └── world/
│       ├── layout.ts        ★ 다시초 배치 상수 · 충돌 박스 · spawn
│       ├── DasiSchoolField.tsx  씬 조립 · 조명 · 라벨
│       ├── SchoolBuilding.tsx   본관(날개·돔 탑) · 강당
│       ├── Playground.tsx       잔디 운동장 · 트랙 · 골대 · 농구대
│       ├── SchoolProps.tsx      게양대 · 소녀상 · 주차장 · 창고 · 화단
│       ├── Nature.tsx           나무 · 마을 · 논 · 원경
│       └── Npc.tsx              담임 선생님 · 대화 · 상호작용
├── content/
│   ├── schema.ts            ★ 퀘스트/도감 타입. learning·source가 필수 필드
│   └── sites/bokamri.quests.json
├── ui/                      Hud · Joystick · CameraControls · DialogueBox
├── store/gameStore.ts       레벨 · XP · 대화 · 퀘스트
└── lib/
    ├── input.ts             키보드·조이스틱 (스토어 밖의 가변 객체)
    ├── collision.ts         AABB 충돌
    ├── palette.ts           로우폴리 색 팔레트
    └── taegukgi.ts          태극기 캔버스 텍스처
```

**새 맵을 추가할 때 지켜야 할 계약** (`docs/06-NAJU-WORLD-MAP.md` 5절):
`layout.ts` + 씬 컴포넌트 + 퀘스트 JSON + 레지스트리 한 줄. 그게 전부여야 합니다.
`src/game/player`, `src/game/quest`, `src/ui`를 건드려야 한다면 엔진에
하드코딩된 부분이 있다는 뜻입니다.
