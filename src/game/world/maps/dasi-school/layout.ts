/**
 * 다시초등학교 필드 배치.
 *
 * 좌표계: +X 동, -Z 북(본관 쪽), +Z 남(도로 쪽). 1 unit = 1m.
 *
 * ── 고증 ────────────────────────────────────────────────────
 * 다시초등학교 · 전남 나주시 다시면 다시로 203
 * 1920년 고막원공립보통학교로 개교 → 1931년 다시공립보통학교
 * → 1946년 다시국민학교 → 1990년 회진국민학교 통합
 * → 1999년 다시남초등학교 통합.
 *
 * 배치 근거: **오너가 제공한 항공 영상(2024-10-30)**. 처음에는 "한국 농촌
 * 초등학교의 전형"을 가정해 흙 운동장 하나짜리로 만들었는데, 실제와 크게
 * 달라 전면 수정했습니다. 항공뷰에서 확인된 것:
 *
 *   · 본관은 분홍(살구)빛 벽돌 3층, 흰 수평 띠. 일자형이 아니라 서쪽 날개가
 *     꺾여 들어가고, 두 날개가 만나는 지점에 **은색 돔을 인 원형 탑**이 있음
 *   · 서북쪽에 **빨간 반원통(아치) 지붕 강당**이 별동으로 있음
 *   · 운동장이 둘 — 본관 앞의 **잔디 운동장**, 그 동쪽에 **빨간 우레탄 트랙 +
 *     파란 안쪽 띠 + 흙 인필드**를 갖춘 정식 트랙
 *   · 교정 한가운데 키 큰 활엽수 군락이 크게 자리 잡음
 *   · 서쪽에 주차장, 남쪽에 파란 지붕 창고
 *   · 학교 밖은 들판이 아니라 **다시면 소재지 마을**
 *
 * 치수는 영상에서 눈대중으로 잰 값이라 실측이 아닙니다. 비율은 맞췄습니다.
 * 실측치를 확보하면 이 파일의 상수만 고치면 되고, 다른 코드는 전부 여기를
 * 참조하므로 모델링을 다시 할 필요가 없습니다.
 * ───────────────────────────────────────────────────────────
 */
import { box, type Box } from '@/lib/collision'

/**
 * 플레이어가 돌아다닐 수 있는 범위.
 * 서쪽은 나루터 끝(x -208)까지, 남쪽은 다시역 승강장(z 106)까지, 동쪽은
 * 배나무 밭 끝(x 185)까지 걸어갈 수 있게 넉넉히 열어뒀습니다.
 * 철로 자체(z 106.5~)는 범위 밖입니다.
 */
export const BOUNDS = { minX: -210, maxX: 190, minZ: -34, maxZ: 106 }

export const LAYOUT = {
  /** 본관 동쪽 날개 — 3층, 남향 일자 */
  eastWing: { x: 30, z: -22, w: 58, d: 13, h: 12.5, floors: 3 },
  /** 본관 서쪽 날개 — 살짝 꺾여 들어갑니다 */
  westWing: { x: -36, z: -26, w: 48, d: 13, h: 12.5, floors: 3, rotY: 0.14 },
  /** 두 날개가 만나는 원형 탑 — 은색 돔. 이 학교에서 가장 눈에 띄는 형태 */
  tower: { x: -3, z: -25, r: 8.5, h: 16.5 },
  /** 강당 — 빨간 반원통 지붕 별동. 90° 돌려 서쪽 날개 옆(서쪽)에 붙였습니다 */
  hall: { x: -74, z: -26, w: 30, d: 21, h: 8, rotY: Math.PI / 2 },
  /** 파란 지붕 창고 */
  shed: { x: -54, z: 42, w: 16, d: 9, h: 5 },

  /** 스쿨버스 — 다시역 앞 도로 맞은편(북쪽, 학교 쪽). 도로와 나란하던 걸
   *  90도 돌려 도로를 마주 보고 나란히 줄 세웠습니다(직각 주차 모양) */
  buses: [
    { x: 79, z: 50, rotY: -Math.PI / 2 },
    { x: 85, z: 50, rotY: -Math.PI / 2 },
  ] as ReadonlyArray<{ x: number; z: number; rotY: number }>,

  /** 잔디 운동장 — 본관 바로 앞 */
  turf: { x: -18, z: 2, w: 64, d: 40 },
  /** 트랙 — 잔디 운동장 동쪽. 빨간 우레탄 + 파란 안쪽 띠 + 흙 인필드
   *  본관 동쪽 날개(z -28.5 ~ -15.5)와 겹치지 않도록 z를 6→12로 내렸습니다. */
  track: { x: 52, z: 12, rx: 39, rz: 25, laneWidth: 7, apronWidth: 2.6 },
  /** 축구 골대 (트랙 인필드 남/북) */
  goals: [
    { x: 52, z: -7, facing: 1 },
    { x: 52, z: 31, facing: -1 },
  ],
  /** 농구 골대 */
  hoops: [
    { x: 30, z: 24 },
    { x: 74, z: 0 },
  ],

  /** 국기게양대 */
  flagpole: { x: 8, z: -14 },
  /** 책 읽는 소녀상 — 서쪽 화단 옆 */
  statue: { x: -76, z: 4 },
  /** 정문 */
  gate: { x: -60, z: 50 },

  /**
   * 플레이어 시작 지점 — 잔디 운동장 한복판, 본관을 바라보고 섭니다.
   *
   * 카메라가 캐릭터 뒤 24m에 서므로, 시작 지점은 남쪽 경계에서
   * 그만큼 안쪽이어야 카메라가 담장 밖으로 빠지지 않습니다.
   */
  spawn: { x: -4, z: 18 },
  /** 첫 퀘스트 NPC (담임 선생님) — 시작하자마자 `!`가 보이는 자리 */
  teacher: { x: 3, z: 11 },
  /** 교장선생님 — 서쪽 날개 정문 캐노피 앞. westWing이 rotY만큼 꺾여 있어
   *  좌표도 그만큼 틀어 계단 앞에 자연스럽게 서게 했습니다 */
  principal: { x: -34, z: -12 },
} as const

/** 교정 한가운데 활엽수 군락 — 항공뷰에서 가장 눈에 띄는 초록 덩어리 */
export const BIG_TREES: ReadonlyArray<[x: number, z: number, scale: number]> = [
  // 시작 지점에서 본관 쪽 시야를 막지 않도록 남서쪽에 모았습니다.
  // 항공뷰에서도 큰 나무들은 잔디 운동장의 남쪽·서쪽 가장자리에 몰려 있습니다.
  [-62, 22, 1.2],
  [-54, 32, 1.35],
  [-44, 26, 1.1],
  [-48, 42, 1.25],
  [-34, 38, 1.15],
  [-24, 44, 1.2],
  [-64, 38, 1.05],
  [-12, 46, 1.0],
  [8, 44, 1.1],
]

/** 본관 앞 향나무 줄 */
export const PINE_ROW: ReadonlyArray<[x: number, z: number, scale: number]> = [
  [12, -13, 1.0],
  [20, -13.4, 1.15],
  [28, -13, 0.92],
  [36, -13.3, 1.08],
  [44, -13, 0.98],
  [-52, -16, 1.1],
  [-58, -12, 0.95],
  [-46, -12, 1.05],
]

/** 화단 */
export const FLOWER_BEDS: ReadonlyArray<[x: number, z: number, w: number, d: number]> = [
  [26, -12, 26, 2.6],
  [-20, -14, 16, 2.6], // 서쪽 날개 현관 캐노피를 가리지 않도록 오른쪽(동쪽)으로 이동
  [-76, 10, 3, 14],
]

/** 마을 집들 — 학교 밖. [x, z, 폭, 깊이, 높이, 지붕색인덱스] */
export const VILLAGE: ReadonlyArray<[number, number, number, number, number, number]> = [
  // 남쪽 (도로 건너)
  [-70, 72, 11, 9, 5, 0], [-52, 78, 13, 10, 5.5, 1], [-34, 70, 10, 8, 4.5, 2],
  [-16, 76, 12, 9, 5, 3], [2, 70, 9, 8, 4.5, 0], [20, 78, 14, 10, 6, 1],
  [40, 71, 10, 9, 5, 2], [58, 77, 12, 9, 5.5, 3], [76, 70, 11, 8, 5, 0],
  [-60, 92, 12, 9, 5, 2], [-30, 95, 14, 10, 6.5, 1], [8, 93, 11, 9, 5, 3],
  // 다시역 좌우의 집 두 채는 뺐습니다 — 역이 가려 보였습니다
  // 서쪽
  [-104, 40, 12, 10, 5.5, 1], [-108, 16, 10, 9, 5, 3], [-102, -8, 13, 10, 6, 0],
  [-110, -34, 11, 9, 5, 2],
  // 동쪽
  [112, 34, 12, 9, 5.5, 3], [116, 8, 10, 8, 5, 0], [110, -20, 13, 10, 6, 1],
  // 북쪽 (본관 뒤편)
  [-70, -70, 12, 9, 5, 2], [-40, -76, 11, 9, 5, 0], [10, -72, 13, 10, 6, 3],
  [46, -78, 10, 8, 4.5, 1], [80, -70, 12, 9, 5.5, 2],
]

/**
 * 영산강 — 마을 서쪽을 흐릅니다. 실제 강폭·유로는 아니고, 학교에서
 * 서쪽을 보면 강이 있다는 인상만 주는 배경입니다. 황포돛배가 이 위를 오갑니다.
 */
export const RIVER = { x: -210, width: 38, zFrom: -260, zTo: 180 }
/** 황포돛배가 오가는 남북 구간 (강 중심선 기준) */
export const BOAT_ROUTE = { zFrom: -220, zTo: 140, period: 55 }

/**
 * 철로 — 학교 남쪽, 호남선. 실제로 다시초등학교 남쪽에 다시역과 철길이
 * 있습니다. 동서로 곧게 뻗은 선 하나로 단순화했습니다.
 */
export const RAILWAY = { z: 110, width: 7, xFrom: -220, xTo: 220 }
/** 기차가 오가는 동서 구간 (철로 중심선 기준) */
export const TRAIN_ROUTE = { xFrom: -150, xTo: 150, period: 45 }
/** 다시역 — 철로 북쪽(학교 쪽) 승강장 옆. 학교 동쪽으로 옮겨 트랙에
 *  가리지 않고 학교에서도 잘 보이게 했습니다 */
export const STATION = { x: 82, z: 96, w: 14, h: 4.2, d: 7 }
/** 역사와 철로 사이 승강장 */
export const PLATFORM = { x: STATION.x, z: 103, w: STATION.w + 2, d: 7 }

/**
 * 배나무 밭 — 학교 정동쪽, 동쪽 마을 집들 뒤(더 동쪽)에 놓았습니다.
 * 나주는 배로 유명한 고장입니다(나주배).
 */
export const ORCHARD = { xFrom: 128, xTo: 185, zFrom: -32, zTo: 32 }

/**
 * 충돌 박스. 통과할 수 없는 것만 넣습니다.
 * 나무·화단처럼 넘어가도 무방한 것은 굳이 막지 않습니다 —
 * 조작 답답함이 고증 정확도보다 비쌉니다.
 */
export const COLLIDERS: readonly Box[] = [
  box(LAYOUT.eastWing.x, LAYOUT.eastWing.z, LAYOUT.eastWing.w, LAYOUT.eastWing.d),
  box(LAYOUT.westWing.x, LAYOUT.westWing.z, LAYOUT.westWing.w + 4, LAYOUT.westWing.d + 4),
  box(LAYOUT.tower.x, LAYOUT.tower.z, LAYOUT.tower.r * 2, LAYOUT.tower.r * 2),
  // 강당을 90° 돌렸으므로 충돌 박스도 폭·깊이를 바꿔 넣습니다
  box(LAYOUT.hall.x, LAYOUT.hall.z, LAYOUT.hall.d, LAYOUT.hall.w),
  box(LAYOUT.shed.x, LAYOUT.shed.z, LAYOUT.shed.w, LAYOUT.shed.d),
  box(LAYOUT.statue.x, LAYOUT.statue.z, 2.2, 2.2),
  box(LAYOUT.flagpole.x, LAYOUT.flagpole.z, 1.6, 1.6),
  box(STATION.x, STATION.z, STATION.w, STATION.d),
  // 버스가 90도 돌아 있으므로(직각 주차) 충돌 박스도 가로·세로를 바꿔 넣습니다
  ...LAYOUT.buses.map((b) => box(b.x, b.z, 2.6, 7.2)),
]
